#include "enemy.h"

/*int shoot(enemy * self)
{
    //x is the direction, as is y. 0,0 is assumed the be the bottom right corner

    switch(self->fireDir)
    {
        case NORTH:
            writeLog("north");
            break;
        case SOUTH:
            writeLog("south");
            break;
        case EAST:
            writeLog("east");
            break;
        case WEST:
            writeLog("west");
            break;
    }

    return 0;
}*/
